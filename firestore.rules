rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Allow anyone to read portfolio data (public website)
    match /portfolio/{document=**} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
